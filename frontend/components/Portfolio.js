import styled from "styled-components";
import Section, { Paragraph, InnerSection, BufferDiv } from "./styles/Section";

const Portfolio = () => (
	<Section id="portfolio">
		<InnerSection>
			<Paragraph>Gallery goes here.</Paragraph>
		</InnerSection>
	</Section>
);

export default Portfolio;
